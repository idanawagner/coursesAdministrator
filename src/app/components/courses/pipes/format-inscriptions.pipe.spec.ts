import { FormatInscriptionsPipe } from './format-inscriptions.pipe';

describe('FormatInscriptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatInscriptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
